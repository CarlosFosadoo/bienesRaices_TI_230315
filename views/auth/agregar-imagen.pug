extends ../layout/admin

block styles
    link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/dropzone/5.9.3/dropzone.css")
    meta(name="csrf-token" content= csrfToken)

block contenido
    .py-10
        h1.text-4xl.my-10.font-extrabold.text-center.text-blue-500 Bienes 
            span.font-normal Raíces  
        h2.text-center.text-2xl.font-extrabold= pagina

        div(class="bg-blue-900 py-8 px-4 shadow-xl shadow-blue-200 rounded-lg mx-auto max-w-4xl my-10 md:px-10")
            form#imagen(class="dropzone border-dashed border-2 border-blue-300 w-full min:h-96 h-auto rounded flex flex-col justify-center items-center" method="POST" action=`/auth/registro/${usuario.id}`)
                input(type="hidden" name="_csrf" value= csrfToken)  
                input(type="hidden" name="context" value="registro")
            button#publicar(class="mt-5 w-full py-3 bg-white hover:bg-blue-500 hover:text-white text-blue-900 font-bold uppercase rounded-lg cursor-pointer" type="button") Subir imagen  

block scripts 
    script(src="/js/agregarImagen.js")
